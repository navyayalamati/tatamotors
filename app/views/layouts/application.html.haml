!!!
%html{"ng-app" => 'myApp'}
  %head
    %title Detroit-Tatamotors
    
    = stylesheet_link_tag    'application'
    = javascript_include_tag 'application'
    = csrf_meta_tags
  .body
    .row
      = render '/shared/banner'
    .row
      - if controller_name == 'passenger_vehicles'
        %br
        .container
          = render '/shared/menu'
          = yield
      - else 
        = yield
